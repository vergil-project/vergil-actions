
# Release 2.1.32 (2026-09-01)

## Bug fixes

- **evidence gate fails red on a failed/skipped leg (#338) (#878)**

## Chores

- **sync .gitignore to the canonical baseline (#869)**
Append the baseline lines this repo was missing (build-sanitize/, the C++ ASan/UBSan out-of-tree build dir) so .gitignore is a verbatim superset of vergil_tooling.data/gitignore.baseline again. Existing local entries are preserved. Part of epic vergil-project/.github#322.

- **sync the vergil-managed .gitignore block (#871)**
Convert this repo's .gitignore to the composed vergil-managed block (base + primary language) — epic vergil-project/.github#325. Foreign-language ignore rules are removed; genuine repo-local entries are preserved outside the fence. Produced by the fleet migration sweep (task #2927).


## Features

- **setup action outputs versions + primary-version from vergil.toml (#338) (#877)**
