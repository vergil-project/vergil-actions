
# Release 2.1.26 (2026-08-12)

## Documentation

- **document build-command step at parity with system-packages (#830)**
Fill the parity gaps where the sibling [container].system-packages step is documented but build-command is not: the Shared actions list, the ci-test workflow reference, and the repository configuration page. Also add a back-link from the system-packages action page to its build-command sibling. Each addition mirrors the system-packages treatment while capturing the build-command specifics: test-jobs-only, runs after system-packages, fail-closed with no retry, and the NODE_PATH export with its require-only/ESM caveat.


## Features

- **promote CI evidence gate to enforcing (#832)**
Flip the evidence-enforce default from false to true in cd-release.yml (and the two composites' inert enforce defaults, for a self-consistent fail-closed default). Incomplete or failed CI evidence now hard-fails a release before publish — the spec §9 publish invariant. Warning mode remains as a temporary per-repo opt-out (evidence-enforce=false), never a permanent soft gate. Promotion follows a clean ~4-week bake: 54/54 vergil-tooling releases (v2.1.138-191) and 100% bundle attachment across the other cd-release repos within their adoption windows, with complete manifests. Completes T11 of epic vergil-project/.github#140.
