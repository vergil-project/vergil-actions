
# Release 2.1.18 (2026-07-24)

## Documentation

- **explicit CD secrets and semgrep rule-exclusion docs (#788)**
Document vrg-semgrep-scan's repeatable --exclude-rule flag, the additive DEFAULT_EXCLUDED_RULES fleet baseline, and the temporary fleet-wide github-actions-mutable-action-tag exemption pending vergil-project/.github#194 (with our own @v2.1 refs as a permanent exception). Document that cd-release callers forward explicit least-privilege publishing secrets per ecosystem instead of blanket secrets: inherit; generated cd.yml emits an explicit secrets block or none accordingly.
